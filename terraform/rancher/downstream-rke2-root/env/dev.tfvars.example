# Cluster naming
prefix                = "demo"
workload_cluster_name = "demo-rke2"

# RKE2 / machine settings
instance_type               = "t3.large"
workload_kubernetes_version = "v1.35.3+rke2r3"
control_plane_quantity      = 1
worker_quantity             = 1
cluster_ready_wait_duration = "90s"
windows_prefered_cluster    = false
# Existing AWS IAM instance profile name for downstream EC2 nodes. Use the name, never the ARN.
downstream_node_instance_profile_name = "demo-downstream-nodes"
# Optional: disable the post-creation cluster-scoped IMDS fix if you manage metadata options outside Terraform.
# enable_cluster_scoped_imds_fix = true

# Rancher server access
rancher_insecure = false

# Choose one credential mode:
# 1. Reuse an existing Rancher cloud credential:
# cloud_credential_id = "cattle-global-data:cc-xxxxx"
#
# 2. Or create a new Rancher cloud credential from AWS keys:
access_key = "YOUR_AWS_ACCESS_KEY"
secret_key = "YOUR_AWS_SECRET_KEY"

# Optional dedicated network for the downstream cluster.
# Leave all five unset to reuse aws-root remote state instead.
# aws_region              = "eu-west-3"
# aws_zone                = "eu-west-3a"
# aws_vpc_id              = "vpc-xxxxxxxx"
# aws_subnet_id           = "subnet-xxxxxxxx"
# ec2_security_group_name = "demo-sg"
