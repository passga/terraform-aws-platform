# Reuse the downstream kubeconfig generated by downstream-rke2-root
kubeconfig_path = "../../rancher/downstream-rke2-root/infra-aws-test-kubeconfig.yaml"

# Optional: pin cert-manager chart version
cert_manager_version = "1.5.3"
